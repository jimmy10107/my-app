# ---------------------------------------------------------------------------
# 伺服器
# ---------------------------------------------------------------------------
PORT=4001
FRONTEND_URL=http://localhost:5173

# ---------------------------------------------------------------------------
# Supabase（在 Supabase 專案 Settings > API 取得）
# service role key 只能放在後端，絕對不要放進前端或 commit 進 git
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------------------------------------------------------------------------
# LINE LIFF（在 LINE Developers Console 建立 Provider > Channel（LINE Login）
# 並在 Channel 內新增 LIFF App 後取得）
# ---------------------------------------------------------------------------
LIFF_CHANNEL_ID=your-liff-channel-id

# ---------------------------------------------------------------------------
# 後台管理（先用簡單密碼即可，之後可升級為多帳號）
# ---------------------------------------------------------------------------
ADMIN_PASSWORD=change-me
ADMIN_JWT_SECRET=change-me-too

# ---------------------------------------------------------------------------
# 人流感測裝置（ESP32 + VL53L1X）回報用的共用密鑰
# ---------------------------------------------------------------------------
VISIT_DEVICE_SECRET=change-me-device-secret
